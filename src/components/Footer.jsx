import React from "react";

export default function Footer() {
  return (
    <footer>
      © {new Date().getFullYear()} — Crafted with data, coffee, and a lot of
      Power Query.
    </footer>
  );
}
